  //Q18
  let bestLocations = ["Germany", "America", "Finland", "Nigera", "Bangladesh"];

  //  Print your array in its original order.
  console.log(bestLocations);

  //  Print your array in alphabetical order without modifying the actual list.
  console.log([bestLocations].sort())

  //  Show that your array is still in its original order by printing it.
  console.log(bestLocations);

  //  Print your array in reverse alphabetical order without changing the order of the original list.
  console.log([bestLocations].sort().reverse());

  //  Show that your array is still in its original order by printing it again.
  console.log(bestLocations);

  //  Reverse the order of your list. Print the array to show that its order has changed.
  console.log(bestLocations.reverse());

  //  Reverse the order of your list again. Print the list to show itâ€™s back to its original order.
  console.log(bestLocations.reverse());

  //  Sort your array so itâ€™s stored in alphabetical order. Print the array to show that its order has been changed.
  console.log(bestLocations.sort());

  //  Sort to change your array so it stored in reverse alphabetical order. Print the list to show that its order has changed.
  
  console.log(bestLocations.sort().reverse());